import { ModulePlaceholder } from "@/components/module-placeholder";

export default function FinancePage() {
  return (
    <ModulePlaceholder
      title="Finance"
      subtitle="Plaid + Manual"
      phase="Phase 4"
      description="Vehicle/personal debt, debt-free projection, spending categories, bills, recurring detection, net worth, cash runway, house + ring savings, tax estimate, off-season cash flow gap."
    />
  );
}
