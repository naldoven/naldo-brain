import { ModulePlaceholder } from "@/components/module-placeholder";

export default function HealthPage() {
  return (
    <ModulePlaceholder
      title="Health"
      subtitle="Apple Health + Manual"
      phase="Phase 5"
      description="Sleep + streak, gym sessions + streak, steps, RHR, weight, daily mood/energy 1-5, hours worked, HRV trend. Burnout triangle alerts."
    />
  );
}
