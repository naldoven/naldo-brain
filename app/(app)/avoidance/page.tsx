import { ModulePlaceholder } from "@/components/module-placeholder";

export default function AvoidancePage() {
  return (
    <ModulePlaceholder
      title="Avoidance Radar"
      phase="Phase 1 + 2"
      description="Auto-flagging (7+ days untouched), manual flag toggle, escalation via WhatsApp (Phase 2). The unique psychological hack — surface what you're dodging."
    />
  );
}
