import { ModulePlaceholder } from "@/components/module-placeholder";

export default function OverviewPage() {
  return (
    <ModulePlaceholder
      title="Overview"
      subtitle="Welcome back, Naldo"
      phase="Phase 1"
      description="Morning briefing, avoidance radar, quick metrics (revenue/debt/runway/sleep), today's snapshot."
    />
  );
}
